# Convert from Fahrenheit to Celsisus Temparature
def to_celsius(x):
  return (x - 32) * 5/9

for x in range(0,101,10):
  print("{:>3} F | {:>6.2f} C".format(x,to_celsius(x)))
  
  # >3 means that we want the numbers to be aligned to the right for a total of 3 spaces.

  # >6.2f means print the number after 2 decimal places and we want to align it to the right at six spaces.
